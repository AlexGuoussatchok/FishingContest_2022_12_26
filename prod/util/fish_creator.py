import random
from prod.model.entity.perch import Perch

class FishCreator:

    @staticmethod
    def create_fish(number_fishes_caught):

        FISH_NAMES = ("perch", "ruffe", "roach", "cancian carp")

        if Fish.fish_name == "perch":
            min_length, max_length, min_weight, max_weight = 150, 300, 200, 300
        if Fish.fish_name == "perch":
            min_length, max_length, min_weight, max_weight = 150, 300, 200, 300
        if Fish.fish_name == "perch":
            min_length, max_length, min_weight, max_weight = 150, 300, 200, 300

        list_of_caught_fish = []


